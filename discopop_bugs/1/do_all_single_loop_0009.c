

int quizzical(int* a, int* b, int* c, int* d);
int recursing();
int sweet(int* a, int b, int* c, int* d);
int heuristic(int* a, int* b, int c, int d, int* e);
int crazy(int* a, int* b, int* c);
int frosty(int* a, int* b, int* c, int* d, int e);
int great(int a);
int jovial(int* a, int b, int* c);
int pensive();
int romantic();


void do_all()
{
    /*initialize random variables*/
    int mh8[6];
    int jFX = 100;
    int gHf[6];
    int ha = 78;
    int i = 36;
    int nF5 = 77;
    int vg = 38;
    

    /*initialize counter variables*/
    
    int x;
    
    // Original loop
    // DiscoPop DOES NOT detect
    for (x = 0; x < 6; x = x + 1)
    {
        
        mh8[x] =  vg + 18 + 5 * nF5;
        jFX =  great(i) - recursing() + 12 / ha / romantic();
        
    }

    // Parameterized function removed
    // DiscoPop DOES detect as do_all
    for (x = 0; x < 6; x = x + 1)
    {
        
        mh8[x] =  vg + 18 + 5 * nF5;
        jFX =  recursing() + 12 / ha / romantic();
        
    }

    // All functions removed
    // DiscoPop DOES detect as do_all
    for (x = 0; x < 6; x = x + 1)
    {
        
        mh8[x] =  vg + 18 + 5 * nF5;
        jFX =   12 / ha;
        
    }
    
}

int quizzical(int* a, int* b, int* c, int* d)
{
    int rRXwZcc = *b + *d + *a + *c + -12;
    if (rRXwZcc == 0) {return 82;}
    return rRXwZcc;
}
int recursing()
{
    int KhukLy =  83;
    if (KhukLy == 0) {return 9;}
    return KhukLy;
}
int sweet(int* a, int b, int* c, int* d)
{
    int GWbR = b + *c + *d + *a + 40;
    if (GWbR == 0) {return -71;}
    return GWbR;
}
int heuristic(int* a, int* b, int c, int d, int* e)
{
    int ZM = c + d + *b + *e + *a + -33;
    if (ZM == 0) {return 12;}
    return ZM;
}
int crazy(int* a, int* b, int* c)
{
    int PzU = *a + *c + *b + 82;
    if (PzU == 0) {return -27;}
    return PzU;
}
int frosty(int* a, int* b, int* c, int* d, int e)
{
    int vHaO = *a + e + *c + *b + *d + -46;
    if (vHaO == 0) {return 54;}
    return vHaO;
}
int great(int a)
{
    int csCT = a + -47;
    if (csCT == 0) {return -15;}
    return csCT;
}
int jovial(int* a, int b, int* c)
{
    int Jm = *c + *a + b + -29;
    if (Jm == 0) {return 75;}
    return Jm;
}
int pensive()
{
    int Gfqd =  7;
    if (Gfqd == 0) {return -21;}
    return Gfqd;
}
int romantic()
{
    int rHdsmtKs =  20;
    if (rHdsmtKs == 0) {return 11;}
    return rHdsmtKs;
}


int main()
{
    do_all();
    return 0;
}