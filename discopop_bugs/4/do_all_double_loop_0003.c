
const int M = 454;
const int N = 454;


int condescending(int* a, int b[M][N]);
int cool(int a[M][N], int* b, int* c, int* d, int e[M][N]);
int clever(int* a, int b, int* c, int d[M][N], int e);
int epic(int* a, int b, int* c, int* d, int e[M][N]);
int reverent(int* a, int b[M][N], int c[M][N], int* d, int* e);
int jolly(int* a, int* b);
int competent(int* a, int b, int c[M][N], int d);
int pensive(int* a);
int compassionate(int* a, int* b, int c[M][N]);
int dreamy(int a[M][N], int* b, int c);


void do_all()
{
    /*initialize random variables*/
    int e[454][454];
    int mnn = 87;
    int z = 61;
    int pS = 70;
    int t = 4;
    int yL = 68;
    int jR[454][454];
    

    /*initialize counter variables*/
    
    int p;
    int v;
    

    
    for (p = 0; p < 454; p = p + 1)
    {
        for (v = 0; v < 454; v = v + 1)
        {
            /*random operations*/
            t =  15 - e[p][v] + yL / pS + e[p][v] / z + 15;
            jR[p][v] =  t + z + pS;
            pS =  1 - yL - jR[p][v] * yL / 1 + yL - 6;
            e[p][v] =  7 + e[p][v];
            z =  18;
            z =  10 / z / z - z / 9 * z;
            e[p][v] =  18 + yL - yL / 11 - 17 * e[p][v] * yL;
            e[p][v] =  14 - 6 * 1 / 17 * jR[p][v];
            jR[p][v] =  yL * t + 19 + mnn;
            pS =  e[p][v] * t * pS - yL;
            z =  mnn;
            mnn =  yL + yL * mnn - mnn * 20;
            mnn =  e[p][v] / yL - yL - e[p][v] + yL;
            t =  e[p][v] - 4 / pS;
            e[p][v] =  e[p][v] / 1 + mnn / 9 + jR[p][v] * jR[p][v] / z;
            yL =  e[p][v] - 10 - t * yL + mnn / yL;
            pS =  yL;
            mnn =  9 + z + 8 * pS / 12 + pS;
            
        }
    }
    
}

int condescending(int* a, int b[M][N])
{
    int XYtwigCF = *a + b[0][0] + 27;
    if (XYtwigCF == 0) {return 50;}
    return XYtwigCF;
}
int cool(int a[M][N], int* b, int* c, int* d, int e[M][N])
{
    int iFaeQeah = *d + a[0][0] + *b + e[0][0] + *c + -73;
    if (iFaeQeah == 0) {return -36;}
    return iFaeQeah;
}
int clever(int* a, int b, int* c, int d[M][N], int e)
{
    int tuJzk = *a + b + *c + e + d[0][0] + -64;
    if (tuJzk == 0) {return 33;}
    return tuJzk;
}
int epic(int* a, int b, int* c, int* d, int e[M][N])
{
    int E = e[0][0] + b + *d + *a + *c + -59;
    if (E == 0) {return -4;}
    return E;
}
int reverent(int* a, int b[M][N], int c[M][N], int* d, int* e)
{
    int RIsTpkl = *e + b[0][0] + c[0][0] + *d + *a + 63;
    if (RIsTpkl == 0) {return -99;}
    return RIsTpkl;
}
int jolly(int* a, int* b)
{
    int B = *b + *a + -95;
    if (B == 0) {return -86;}
    return B;
}
int competent(int* a, int b, int c[M][N], int d)
{
    int CClZ = *a + b + c[0][0] + d + 7;
    if (CClZ == 0) {return 57;}
    return CClZ;
}
int pensive(int* a)
{
    int jm = *a + 47;
    if (jm == 0) {return -52;}
    return jm;
}
int compassionate(int* a, int* b, int c[M][N])
{
    int ivMKG = c[0][0] + *a + *b + 80;
    if (ivMKG == 0) {return -89;}
    return ivMKG;
}
int dreamy(int a[M][N], int* b, int c)
{
    int dZYLQUP = c + a[0][0] + *b + -73;
    if (dZYLQUP == 0) {return -85;}
    return dZYLQUP;
}


int main()
{
    do_all();
    return 0;
}